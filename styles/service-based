Each service represents a business domain
The most pragmatic architecture
Services can be layered
Each service can have its own UI, share UI or have no UI
Up to 12 coarse-grained services
Uses REST and DB partitioning
Use after layered monolith, until the scaling and elasticity become a problem